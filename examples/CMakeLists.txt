# Examples will be added later
# For now, just print a message

message(STATUS "Examples directory configured (no examples yet)")

# Uncomment when we add examples:
# add_subdirectory(01_HelloTriangle)